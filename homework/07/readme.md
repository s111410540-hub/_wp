# 作業AI使用說明
全程皆使用antigravity完成
我:
```
https://github.com/ccc114b/_wp/issues/17
在07的資料夾底下完成作業要求
```
gemini:
```
Read URL: https://github.com/ccc114b/_wp/issues/17
Viewed content.md:1-800
Listed directory _wp
Listed directory homework
Listed directory 07
Viewed content.md:800-1011
Created ex1.js
Created ex2.js
Created ex3.js
Created ex4.js
Created ex5.js
Created ex6.js
Created ex7.js
Created ex8.js
Created ex9.js
Created ex10.js

我已經根據 GitHub issue #17 的內容，在 `d:\Website\NQU\_wp\homework\07` 資料夾底下為你建立了 10 個 JavaScript 檔案 (`ex1.js` 到 `ex10.js`)，分別對應那 10 題練習。

以下是每個檔案的簡要說明：

*   **`ex1.js`**：練習物件屬性的存取（使用點符號與中括號）。
*   **`ex2.js`**：物件的解構賦值（從 `req.body` 取出 `title` 和 `content`）。
*   **`ex3.js`**：練習使用 `forEach` 遍歷陣列並用樣板字串 (Template Literals) 拼接 HTML。
*   **`ex4.js`**：了解 URL 參數在 JS 中的對應形式（字典與動態新增屬性）。
*   **`ex5.js`**：實作非同步設計裡的 Callback 函數傳參機制。
*   **`ex6.js`**：如何將 JSON 字串解析並提取出所需屬性。
*   **`ex7.js`**：建立假函數 `fakeGet` 來模擬 `db.get` 查資料庫與回呼的形式。
*   **`ex8.js`**：練習在樣板字串中用三元運算子寫入簡單邏輯（例如 `user ? user : "Stranger"`）。
*   **`ex9.js`**：陣列遍歷加上字串裁切（`substring`）與字串相加，模擬 `ORDER BY` 以及 `substr` 邏輯。
*   **`ex10.js`**：熟悉 Node.js 裡常見的錯誤優先回呼模式（Error-First Callback Pattern），實作 `checkAdmin`。

你現在可以直接進入該目錄並用 `node ex1.js` 這樣的指令逐一測試這些程式碼了！
```